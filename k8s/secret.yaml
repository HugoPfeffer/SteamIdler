apiVersion: v1
kind: Secret
metadata:
  name: steamidler-credentials
type: Opaque
stringData:
  # Replace placeholder values with your actual configuration before applying.
  # See .env.example for documentation on each variable.
  STEAM_USERNAME: "REPLACE_ME"
  STEAM_PASSWORD: "REPLACE_ME"
